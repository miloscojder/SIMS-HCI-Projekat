<Window x:Class="Projekat.Prescriptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekat"
        mc:Ignorable="d"
        Title="Prescriptions" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding Prescription}" IsReadOnly="True"
                  SelectionMode="Single" AutoGenerateColumns="False" Margin="88,58,63.6,116">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Width="80" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Medicine" Width="100" Binding="{Binding Medicine}"/>
                <DataGridTextColumn Header="Quantity" Width="100" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="Instruction" Width="150" Binding="{Binding Instruction}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Prescribe" Click="Prescribe" FontSize="18" HorizontalAlignment="Left" Margin="567,354,0,0" VerticalAlignment="Top" Height="37" Width="132" Background="#FFB5CDE2"/>
        <Label Content="Prescriptions" FontSize="25" HorizontalAlignment="Left" Margin="327,15,0,0" VerticalAlignment="Top" Width="174"/>



    </Grid>
</Window>
