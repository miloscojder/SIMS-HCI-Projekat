<Window x:Class="Projekat.HospitalAssessment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekat"
        mc:Ignorable="d"
        Title="HospitalAssessment" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="hospitalAssessmentDataGrid" Margin="0,0,0,267" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                    Binding="{Binding HospitalName}" />
                <DataGridTextColumn Header="Hospital Adress"
                    Binding="{Binding HospitalAdress}" />
                <DataGridTextColumn Header="hospitalFeedback"
                    Binding="{Binding hospitalFeedback}" />
                <DataGridTextColumn Header="gradesOfThisHospital"
                    Binding="{Binding gradesOfThisHospital}" />
               
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
